# Copia a apps/web/.env (gitignorado). TODO lo VITE_* acaba en el bundle: aquí
# solo va lo PÚBLICO — URL del proyecto y publishable key. Jamás una secret key.
# Local (supabase start):
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=REEMPLAZAR-con-el-anon-key-local
VITE_API_URL=http://127.0.0.1:3000
