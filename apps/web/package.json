{
  "name": "@ladino/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vite + React + React Router",
  "exports": {
    ".": {
      "ladino-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run"
  }
}
