# /etc/ladino/api.env — secretos del servicio, en el HOST, chmod 600, fuera de git.

# Pooler de Supabase en MODO TRANSACCIÓN (puerto 6543). No el 5432 directo:
# la API usa `set local` y `prepare: false` contando con este modo (S0.5).
#
# COMO `ladino_api`, NUNCA como `postgres` (ADR-0031): el rol dedicado no tiene
# BYPASSRLS y las policies lo acotan al tenant del actor. Conectar como
# postgres «funciona» y es exactamente el hallazgo F-15. La contraseña la fija
# el operador en el remoto (infra/README.md §Roles de servicio).
DATABASE_URL=postgresql://ladino_api.udacvwnhwpsdzbouhqhl:REEMPLAZAR@aws-0-REGION.pooler.supabase.com:6543/postgres

# Verificación de JWT en modo jwks (ES256): solo la clave PÚBLICA del proyecto.
LADINO_AUTH_MODE=jwks
SUPABASE_JWKS_URL=https://udacvwnhwpsdzbouhqhl.supabase.co/auth/v1/.well-known/jwks.json
SUPABASE_AUTH_ISSUER=https://udacvwnhwpsdzbouhqhl.supabase.co/auth/v1

# Storage (fotos de productos, logos, comprobantes). Sin estas dos, la API responde que no hay
# almacenamiento configurado. La clave es la SECRETA del proyecto: la nueva (`sb_secret_…`) o la
# `service_role` heredada (JWT) — el cliente manda cada una en su cabecera (apps/api/src/storage.ts).
SUPABASE_STORAGE_URL=https://udacvwnhwpsdzbouhqhl.supabase.co/storage/v1
SUPABASE_STORAGE_KEY=REEMPLAZAR

# Opcionales (valores por defecto): peticiones/min por usuario y plazo por petición.
# REQUEST_TIMEOUT_MS debe quedar MUY por debajo de los 15 min del reaper de idempotencia.
RATE_LIMIT_PER_MINUTE=300
REQUEST_TIMEOUT_MS=30000
