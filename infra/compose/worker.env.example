# /etc/ladino/worker.env — en el HOST, chmod 600, fuera de git.
# Como `ladino_worker` (ADR-0031): GRANT solo sobre outbox e idempotency_keys.
DATABASE_URL=postgresql://ladino_worker.igpfrwdgmicgyirwdbgs:REEMPLAZAR@aws-0-REGION.pooler.supabase.com:6543/postgres
WORKER_INTERVAL_MS=2000
